Flask==3.0.0
python-docx==1.1.0
openpyxl==3.1.2
beautifulsoup4==4.12.2
pdfplumber==0.10.3
Jinja2==3.1.2
Werkzeug==3.0.1
translators>=5.9

# 可选依赖（PDF 生成需要，需要 C 编译环境）
# Windows: 无需额外操作
# macOS: xcode-select --install
# Linux: sudo apt install libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev
# weasyprint>=60.0
